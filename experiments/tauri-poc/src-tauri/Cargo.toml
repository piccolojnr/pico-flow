[package]
name = "flow-linux-tauri-poc"
version = "0.1.0"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.6", features = [] }

[dependencies]
tauri = { version = "2.11", features = ["tray-icon"] }
rdev = "0.5"
serde_json = "1"
